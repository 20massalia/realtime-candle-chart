# Copy to repo root as `.env.mcp` (gitignored). Cursor loads this via mcp.json envFile.
# Dock-launched Cursor does not inherit `export` from ~/.zshrc.

DATABASE_URL=postgres://mcp_reader:<password>@localhost:5432/candles
KUBECONFIG=/Users/<you>/.kube/config
