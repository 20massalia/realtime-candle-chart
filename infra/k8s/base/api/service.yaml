apiVersion: v1
kind: Service
metadata:
  name: api
  labels:
    app.kubernetes.io/name: candle-api
    app.kubernetes.io/component: api
    app.kubernetes.io/version: "0.0.1"
spec:
  selector:
    app.kubernetes.io/name: candle-api
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8080
      targetPort: http
